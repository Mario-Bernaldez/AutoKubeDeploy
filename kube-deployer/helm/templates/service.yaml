apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-deployer.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "kube-deployer.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
