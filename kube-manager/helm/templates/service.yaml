apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-manager.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "kube-manager.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
